<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema del framework para no depender de com.google.android.material.
         El contenido lo dibuja Compose (MaterialTheme). -->
    <style name="Theme.TorrentBox" parent="@android:style/Theme.Material.NoActionBar" />

    <!-- El reproductor usa AppCompat porque el diálogo de Chromecast lo requiere -->
    <style name="Theme.TorrentBox.Player" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="colorPrimary">#7B5BF5</item>
        <item name="colorAccent">#7B5BF5</item>
    </style>
</resources>
